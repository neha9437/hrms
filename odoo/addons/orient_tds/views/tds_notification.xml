<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <!-- <record model="ir.cron" id="training_mail_reminder">
            <field name="name">Training Reminder</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="doall" eval="False"/>
            <field name="model_id" ref="model_training_master"/>
            <field name="state">code</field>
            <field name="code">model.training_mail_reminder()</field>
        </record> -->
    </data>
</odoo>
